diff a/src/components/NearBy.js b/src/components/NearBy.js	(rejected hunks)
@@ -33 +33 @@ class NearBy extends Component {
-            cur_state: "RESTAURANT",
+            cur_state: 'RESTAURANT',
@@ -50,2 +50 @@ class NearBy extends Component {
-            }
-
+            },
@@ -64,6 +63,14 @@ class NearBy extends Component {
-                    this.props.nearby.refetch({ categoryCode: "RESTAURANT", radius: this.state.radius, geolocation: `${latitude},${longitude}`, no: this.state.no }).then(res => {
-                        this.setState({
-                            places: {
-                                ...this.state.places,
-                                RESTAURANT: res.data.places
-                            }
+                    this.props.nearby
+                        .refetch({
+                            categoryCode: 'RESTAURANT',
+                            radius: this.state.radius,
+                            geolocation: `${latitude},${longitude}`,
+                            no: this.state.no,
+                        })
+                        .then(res => {
+                            this.setState({
+                                places: {
+                                    ...this.state.places,
+                                    RESTAURANT: res.data.places,
+                                },
+                            })
@@ -77,2 +83 @@ class NearBy extends Component {
-        }
-        else {
+        } else {
@@ -80,8 +85,16 @@ class NearBy extends Component {
-            this.props.nearby.refetch({ categoryCode: "RESTAURANT", radius: this.state.radius, geolocation: `${latitude},${longitude}`, no: this.state.no }).then(res => {
-                this.setState({
-                    location: { latitude, longitude },
-                    loading: false,
-                    places: {
-                        ...this.state.places,
-                        RESTAURANT: res.data.places
-                    }
+            this.props.nearby
+                .refetch({
+                    categoryCode: 'RESTAURANT',
+                    radius: this.state.radius,
+                    geolocation: `${latitude},${longitude}`,
+                    no: this.state.no,
+                })
+                .then(res => {
+                    this.setState({
+                        location: { latitude, longitude },
+                        loading: false,
+                        places: {
+                            ...this.state.places,
+                            RESTAURANT: res.data.places,
+                        },
+                    })
@@ -99,2 +111,2 @@ class NearBy extends Component {
-                    [cur]: this.props.nearby.places
-                }
+                    [cur]: this.props.nearby.places,
+                },
@@ -115 +126 @@ class NearBy extends Component {
-            cur_state: category
+            cur_state: category,
@@ -117 +127,0 @@ class NearBy extends Component {
-        console.log(category, target)
@@ -120 +130,6 @@ class NearBy extends Component {
-            await this.props.nearby.refetch({ categoryCode: category, radius: this.state.radius, geolocation: `${latitude},${longitude}`, no: this.state.no })
+            await this.props.nearby.refetch({
+                categoryCode: category,
+                radius: this.state.radius,
+                geolocation: `${latitude},${longitude}`,
+                no: this.state.no,
+            })
@@ -129 +143,3 @@ class NearBy extends Component {
-                    <Link to={`/detail?place=${place.placeID}&code=${place.categoryCode}`}>
+                    <Link
+                        to={`/detail?place=${place.placeID}&code=${place.categoryCode}`}
+                    >
@@ -140,5 +156 @@ class NearBy extends Component {
-        return (
-            <div className='tab-content'>
-                {box}
-            </div>
-        )
+        return <div className='tab-content'>{box}</div>
@@ -163,4 +175 @@ class NearBy extends Component {
-                    <img
-                        alt='icon-tab'
-                        src={tabInActive}
-                    />
+                    <img alt='icon-tab' src={tabInActive} />
@@ -189,3 +198 @@ class NearBy extends Component {
-                        <div className='picture-container'>
-                            Loading
-                        </div>
+                        <div className='picture-container'>Loading</div>
@@ -211 +218,3 @@ class NearBy extends Component {
-                            this.genPlace(this.state.places[this.state.cur_state])}
+                            this.genPlace(
+                                this.state.places[this.state.cur_state]
+                            )}
@@ -222 +231 @@ export default compose(
-    graphql(nearBy, { name: "nearby" })
+    graphql(nearBy, { name: 'nearby' })
